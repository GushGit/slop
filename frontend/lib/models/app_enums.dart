enum AppLanguage { russian }

enum GoalMode { weightLoss, maintain, massGain }

enum MealType { breakfast, lunch, snack, dinner }

enum RecipeDifficulty { easy, medium, hard }

enum FridgeViewType { ingredients, readyMeals }
